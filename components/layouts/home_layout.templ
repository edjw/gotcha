package layouts

templ HomeLayout(pageName string) {
	@BaseLayout(pageName) {
		<header>
			<h1>{ pageName }</h1>
		</header>
		<main>
			{ children... }
		</main>
		<footer>
			<p>Footer content</p>
		</footer>
	}
}
